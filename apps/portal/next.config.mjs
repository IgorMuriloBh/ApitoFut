/** @type {import('next').NextConfig} */
const nextConfig = {
  // O white-label por domínio próprio (competicoes.dominio_personalizado)
  // entrará via middleware que resolve o host para /{slug} — planejado no
  // CLAUDE.md como a razão de o portal ser Next.
};

export default nextConfig;
