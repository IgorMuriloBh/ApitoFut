# Em desenvolvimento o Vite faz proxy de /api → localhost:3000 (vite.config.ts),
# então estas variáveis só importam no build de produção.
VITE_API_URL="http://localhost:3000"
VITE_PORTAL_URL="http://localhost:3001"
