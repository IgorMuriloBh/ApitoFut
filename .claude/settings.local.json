{
  "permissions": {
    "allow": [
      "Bash(echo \"--- Downloads/apitofut ---\"; ls -la ~/Downloads/apitofut 2>&1 | head -50; echo \"--- projetos ---\"; ls -la ~/projetos 2>&1 | head; echo \"--- docker ---\"; which docker docker-compose 2>&1; echo \"--- node/npm ---\"; which node npm 2>&1; node --version 2>&1)",
      "Bash(git --version)",
      "Bash(git init *)",
      "Bash(git add *)",
      "Bash(git commit -q -m 'Protótipo, especificação e modelo de dados *)"
    ]
  }
}
